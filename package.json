{
  "name": "nyx",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/src/index.js",
    "compile:ffi": "gcc -shared -o ./FFI/main.so  server/main.c"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/ffi-napi": "^4.0.10",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3"
  }
}
